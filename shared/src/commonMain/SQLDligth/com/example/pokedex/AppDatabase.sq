    CREATE TABLE Pokedex(
    name TEXT NOT NULL PRIMARY KEY,
    url TEXT NOT NULL
    );

        insertPokemon:
        INSERT INTO Pokedex(name, url)
        VALUES (?, ?);

        deleteAllPokemon:
        DELETE FROM Pokedex;

        selectAllPokemon:
        SELECT * FROM Pokedex;